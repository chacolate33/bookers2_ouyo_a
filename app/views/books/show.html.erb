<div class="container">
  <div class="row">

   <div class="col-md-3">

    <h2>User info</h2>
    <%= render 'users/info', user: @book.user, isRoom: @isRoom, room: @room, entry: @entry, roomId: @roomId %>

    <h2 class="mt-3">New book</h2>
    <%= render 'books/form', book: Book.new %>
   </div>

   <div class="col-md-8 offset-md-1">
   <h2>Book detail</h2>

   <%= render 'show', book: @book %>
   
   <%= render 'book_comments/comment', book: @book, book_comment: @book_comment %>
 　
 　<%= form_with model:[@book, @book_comment] do |f| %>
   <%= f.text_area :comment, rows:'5', class: 'w-100' %><br>
   <%= f.submit "送信" %>
   <% end %>
   　
   </div>
  </div>
 </div>